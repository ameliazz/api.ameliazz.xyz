{
	"settings": {
		"allowUndefinedValues": false
	},
	"environments": {
		"production": {
			"hostname": "0.0.0.0",
			"port": "$env('PORT')",
			"prettyJSON": false
		},
		"development": {
			"@mozz:dotenv": ".env.local",
			"hostname": "localhost",
			"port": "$env('PORT')",
			"prettyJSON": true
		}
	}
}
